\usepackage[backend=biber, % Use biber as the backend
            style=authoryear-comp, % Author-year style with compressed citations
            sorting=nyt, % Sort by name, year, title
            maxcitenames=2, % Only show 2 authors in cite before et al.
            giveninits=true]{biblatex} % Use initials for first names
            
            
\addbibresource{../references/global_bibliography.bib}